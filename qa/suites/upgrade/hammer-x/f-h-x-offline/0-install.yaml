roles:
- [mon.a, mon.b, mon.c, osd.0, osd.1, osd.2, client.0]
openstack:
- volumes: # attached to each instance
    count: 3
    size: 10 # GB
tasks:
- install:
    branch: firefly
- ceph:
    fs: xfs
    log-whitelist:
    - reached quota
