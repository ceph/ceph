meta:
- desc: |
   librbd C and C++ api tests
overrides:
  ceph:
    log-ignorelist:
      # TestLibRBD.RemoveFullTryDataPool
      - POOL_FULL
      - is full
      - pool.*full
stress-tasks:
- workunit:
     branch: squid
     clients:
        client.0:
           - rbd/test_librbd.sh
     env:
       RBD_FEATURES: "61"
- print: "**** done rbd/test_librbd.sh 7-workload"
