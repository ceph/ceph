overrides:
  ceph:
    fs: xfs
    conf:
      osd:
        osd objectstore: filestore
        osd sloppy crc: true
        osd op thread timeout: 900
        osd op thread suicide timeout: 1800
  ceph-deploy:
    fs: xfs
    filestore: True
    conf:
      osd:
        osd objectstore: filestore
        osd sloppy crc: true

