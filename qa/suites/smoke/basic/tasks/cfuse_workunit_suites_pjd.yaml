tasks:
- install:
- ceph:
    fs: xfs
    use-ceph-volume: true
    conf:
      mds:
        debug mds: 20
        debug ms: 1
      client:
        debug client: 20
        debug ms: 1
        fuse default permissions: false
        fuse set user groups: true
- ceph-fuse:
- workunit:
    clients:
      all:
        - suites/pjd.sh
