overrides:
  ceph:
    conf:
      client:
        setuser: ceph
        setgroup: ceph
        debug rgw: 20
        debug rgw dedup: 20
  rgw:
    realm: dedup-realm
    zonegroup: default
    zone: default
    storage classes:
      LUKEWARM:
      FROZEN:
