overrides:
  ceph:
    log-whitelist:
      - overall HEALTH_
      - \(CACHE_POOL_NO_HIT_SET\)
      - \(POOL_APP_NOT_ENABLED\)
    conf:
      client:
        rbd op threads: 8
tasks:
- workunit:
    clients:
      client.0:
        - rbd/test_rbd_mirror.sh
