tasks:
  - parallel:
      - io_workload
      - migrate_workload
migrate_workload:
  sequential:
    - exec:
        cluster1.client.0:
          - sleep 60
          - rbd --cluster cluster1 migration execute client.0.0
          - sleep 60
          - rbd --cluster cluster1 migration commit client.0.0
