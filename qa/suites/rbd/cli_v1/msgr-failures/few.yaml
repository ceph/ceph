overrides:
  ceph:
    conf:
      global:
        ms inject socket failures: 5000
        mon client directed command retry: 5
      client:
        rbd op threads: 8
    log-whitelist:
      - \(OSD_SLOW_PING_TIME
