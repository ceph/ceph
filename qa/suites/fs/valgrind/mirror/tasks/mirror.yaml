overrides:
  ceph:
    log-ignorelist:
      - CEPHFS_MIRROR_FAILURE
      - CEPHFS_MIRROR_SNAP_SYNC_FAILURE
    conf:
      mgr:
        debug client: 10

tasks:
  - cephfs_test_runner:
      modules:
        - tasks.cephfs.test_mirroring.TestMirroring
