overrides:
  install:
    extra_system_packages:
      rpm:
        - fio
      deb:
        - fio
tasks:
  - exec:
      host.a:
        - pip3 install pyyaml
  - cephfs_test_runner:
      modules:
        - tasks.cephfs.test_nfs
