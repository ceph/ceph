overrides:
  install:
    exclude_packages:
      - ceph-selinux
    extra_system_packages:
      rpm:
        - fio
      deb:
        - fio
tasks:
  - cephfs_test_runner:
      modules:
        - tasks.cephfs.test_nfs
