meta:
  - desc: "Packaged prometheus and grafana"
overrides:
  cephmetrics:
    group_vars:
      all:
        prometheus:
          containerized: false
        grafana:
          containerized: false
