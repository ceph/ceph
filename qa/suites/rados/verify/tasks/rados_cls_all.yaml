overrides:
  ceph:
    conf:
      osd:
        osd_class_load_list: "*"
        osd_class_default_list: "*"
    log-ignorelist:
      - \(POOL_APP_NOT_ENABLED\)
tasks:
- workunit:
    clients:
      client.0:
        - cls
