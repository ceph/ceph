teuthology:
  postmerge:
    - |
      if yaml.os_type == "rocky" then
        log.debug("not testing on rocky: https://tracker.ceph.com/issues/80294")
        reject()
      end
roles:
- - mon.a
  - mgr.x
  - osd.0
  - client.0
openstack:
- volumes: # attached to each instance
    count: 4
    size: 10 # GB
