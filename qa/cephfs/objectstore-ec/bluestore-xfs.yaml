overrides:
  ceph:
    fs: xfs
    conf:
      osd:
        osd objectstore: bluestore
        osd sloppy crc: true
  ceph-deploy:
    fs: xfs
    filestore: True
    conf:
      osd:
        osd objectstore: bluestore
        osd sloppy crc: true

