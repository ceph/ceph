overrides:
  deepsea:
    storage_profile:
        ceph:
          storage:
            osds:
              /dev/vdb:
                format: bluestore
                encryption: dmcrypt
                wal: /dev/vde
              /dev/vdc:
                format: bluestore
                encryption: dmcrypt
                wal: /dev/vde
              /dev/vdd:
                format: bluestore
                encryption: dmcrypt
                wal: /dev/vde
