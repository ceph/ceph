overrides:
  ceph:
    fs: xfs
    conf:
      osd:
        osd objectstore: bluestore
        osd sloppy crc: true
  ceph-deploy:
    fs: xfs
    bluestore: True
    conf:
      osd:
        osd objectstore: bluestore
        osd sloppy crc: true

