ceph:
  storage:
    osds:
      /dev/vdb:
        format: filestore
        encryption: dmcrypt
        journal: /dev/vde
      /dev/vdc:
        format: filestore
        encryption: dmcrypt
        journal: /dev/vde
      /dev/vdd:
        format: filestore
        encryption: dmcrypt
        journal: /dev/vde
