# There are no libmds_types so use the full mds library for dencoder for now
DENCODER_SOURCES += $(LIBMDS_SOURCES)

DENCODER_DEPS += $(LIBMDS_DEPS)
if WITH_MANTLE
    DENCODER_DEPS += $(LUA_LIBS)
endif # WITH_MANTLE
