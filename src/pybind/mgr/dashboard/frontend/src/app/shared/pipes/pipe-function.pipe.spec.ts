import { PipeFunctionPipe } from './pipe-function.pipe';

describe('PipeFunctionPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFunctionPipe();
    expect(pipe).toBeTruthy();
  });
});
