import { TextToDownloadDirective } from './text-to-download.directive';

describe('TextToDownloadDirective', () => {
  it('should create an instance', () => {
    const directive = new TextToDownloadDirective();
    expect(directive).toBeTruthy();
  });
});
