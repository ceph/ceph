import { FormValidationPipesPipe } from './form-validation-pipes.pipe';

describe('FormValidationPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormValidationPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
