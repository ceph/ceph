version: "2.4"

# If cgroups v2 is disabled then add cgroup fs
services:
        seed:
            volumes:
                - "/sys/fs/cgroup:/sys/fs/cgroup:ro"
        hosts:
            volumes:
                - "/sys/fs/cgroup:/sys/fs/cgroup:ro"
