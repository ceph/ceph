[workspace]
members = [
    "lancedb-c",
    "lancedb-rgw-store",
    "rgw-lancedb",
]
resolver = "2"

# prevent pulling in all debug info for lancedb dependencies
[profile.dev.package."*"]
opt-level = 2
debug = false
