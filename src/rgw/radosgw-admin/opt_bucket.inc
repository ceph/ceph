  BUCKETS_LIST,
  BUCKET_LIMIT_CHECK,
#ifdef WITH_RADOSGW_RADOS
  BUCKET_LINK,
  BUCKET_UNLINK,
#endif
  BUCKET_LAYOUT,
  BUCKET_STATS,
  BUCKET_SUSPEND,
  BUCKET_UNSUSPEND,
#ifdef WITH_RADOSGW_RADOS
  BUCKET_CHECK,
  BUCKET_CHECK_OLH,
  BUCKET_CHECK_UNLINKED,
#endif
  BUCKET_RM,
#ifdef WITH_RADOSGW_RADOS
  BUCKET_REWRITE,
  BUCKET_RESHARD,
#endif
  BUCKET_SET_MIN_SHARDS,
  BUCKET_CHOWN,
#ifdef WITH_RADOSGW_RADOS
  BUCKET_RADOS_LIST,
#endif
  BUCKET_SHARD_OBJECTS,
  BUCKET_OBJECT_SHARD,
