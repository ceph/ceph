overrides:
  ceph:
    valgrind:
      mon: [--tool=memcheck]
      osd: [--tool=memcheck]
      mds: [--tool=memcheck]
  ceph-fuse:
    client.0:
      valgrind: [--tool=memcheck, --leak-check=full, --show-reachable=yes]
